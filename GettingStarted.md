# 3d-printing-model-prompt — Getting Started

**There is nothing to install or run yet.** This repository currently
contains `CLAUDE.md` (this project's own coding-practice standards) and a
blank README — no source code, no dependencies, no Dockerfile, nothing
that boots.

See `Requirements.md` for what's known about this project's intended
purpose and what needs to happen (starting with confirming real scope
with the project owner) before there's anything here to run.

Once real code lands, replace this file with actual instructions
following the pattern used across the rest of the suite this project
belongs to (see e.g. TeamOps's or Shopify-App's `GettingStarted.md`/
README.md for the expected shape: prerequisites, a Windows PowerShell
quick-start block, a matching Linux/Mac bash block, how to run tests, and
a troubleshooting table for the issues people actually hit — missing
dependencies, port conflicts, missing `.env` values, etc.) — and note
that `CLAUDE.md`'s own rule 4 requires this project to be Docker-first
and behave identically on Windows and Linux hosts once it exists.
